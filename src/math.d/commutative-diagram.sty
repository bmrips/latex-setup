\RequirePackage{bmrips/prelude}

\LoadPackageWithOptions{tikz-cd}
\tikzcdset{
    every arrow/.append style = -{Glyph[glyph math command=rightarrow]}
}

% Reset Babel's catcode customisations inside tikz.
\usetikzlibrary{babel}
